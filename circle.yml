test:
  override:
    - echo "test"
deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync ./handle-result s3://s3-eu-west-1.amazonaws.com/tegud-assets/handle-result/ --delete
      - aws s3 sync ./make-request s3://s3-eu-west-1.amazonaws.com/tegud-assets/make-request/ --delete
